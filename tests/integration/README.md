# Integration tests

Integration tests will exercise PostgreSQL migrations, transactional behavior, Docker worker reconciliation, and provider contracts. They must use disposable test resources and remain separate from fast unit tests.
